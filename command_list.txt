Add a main similar to the one in cmd/router/main.go except it does not call the apis and have it stream mock example of how the code should function to the frontend tsx file
as well as that add another webpage that can be accessed from the App.tsx that takes in the api usage and other metrics add a bar graph and other metrics to show the api usage keep style consistent with the app file.